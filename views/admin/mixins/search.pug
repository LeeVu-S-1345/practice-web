mixin search (keyword)
    form(id="form-search")
        .input-group
            input(
                type="text"
                name="keyword"
                class="form-control"
                placeholder="Enter the key"
                value= keyword
            )
            .input-group-append
                button(
                    class="btn btn-success"
                    type="submit"
                ) Search